export const lightNotFound = (light) => {
    return "Light (" + light + ") not found!";
};

export const groupNotFound = (group) => {
    return "Group (" + group + ") not found!"; 
};

export const bridgeNotFound = (bridgeIP) => {
    return "Bridge with IP (" + bridgeIP + ") not found!";
};
